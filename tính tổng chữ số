#include <iostream>
#include <cmath>
using namespace std;
long long tong(long long n)
{
	if (n <= 10)
		return n;
	return (n % 10) + tong(n / 10);
}
int main()
{
	long long a;
	cin >> a;
	cout << tong(a) << endl;
	return 0;
}
