#include <iostream>
using namespace std;
long long giaithua(int a)
{
	if (a == 0)
		return 1;
	else
		return a * giaithua(a - 1);
}
int main()
{
	int a;
	cin >> a;
	cout << giaithua(a) << endl;
	return 0;
}
