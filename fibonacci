#include <iostream>
#include <cmath>
using namespace std;
long long fibonacci(long long n)
{
	if (n <= 1)
		return n;
	return fibonacci(n - 1) + fibonacci(n - 2);
}
int main()
{
	long long a;
	cin >> a;
	cout << fibonacci(a) << endl;
	return 0;
}
