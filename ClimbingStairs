#include <iostream>
using namespace std;
int ClimbingStairs(int n)
{
	if (n == 1 || n == 0)
		return 1;
	return ClimbingStairs(n - 1) + ClimbingStairs(n - 2);
}
int main()
{
	int n;
	cin >> n;
	cout << ClimbingStairs(n) << endl;
}
